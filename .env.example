# MongoDB Connection String (Get this from MongoDB Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/forensiclab?retryWrites=true&w=majority

# Optional: JWT Secret for future auth (not used yet)
JWT_SECRET=audi_forensics_secret_key_123
